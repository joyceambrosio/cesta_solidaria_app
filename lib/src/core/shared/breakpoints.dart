import 'package:flutter/material.dart';

const mobileBreakpoint = 700;
const tabletBreakpoint = 1200;
const maxWidth = 1400.00;

const borderRadiosAll = BorderRadius.all(Radius.circular(30));
const paddingInAll = EdgeInsets.all(15);
